(ns app.reg.all
  (:require
    [app.reg.effects.interval]
;
    [app.reg.events.challenge]
;    [app.reg.events.inc-score]
    [app.reg.events.initialize]
    [app.reg.events.put-log]
    [app.reg.events.tap]
    [app.reg.events.tick]
    [app.reg.events.text-input]
    [app.reg.events.wpm]
;
    [app.reg.subs.challenge]
;    [app.reg.subs.goal-code]
;    [app.reg.subs.goal-text]
;    [app.reg.subs.last-log]
;    ;[app.reg.subs.log-morse-text]
    [app.reg.subs.log-morse-code]
    [app.reg.subs.text-output]
;    [app.reg.subs.predictive-log]
;    [app.reg.subs.score]
;    [app.reg.subs.ticker]
    [app.reg.subs.unit]
  ))
